# kalshi-sdk pulls in: requests, cryptography, python-dotenv, websocket-client.
# Install with: pip install -e ../kalshi_sdk  (run from this dir)
-e ../kalshi_sdk

# Bot-specific:
PyYAML>=6.0
pandas>=2.2
numpy>=1.26
scikit-learn==1.6.1
scipy>=1.11
joblib>=1.3
# ESPN injuries page parsing (pandas.read_html needs a parser + bs4).
lxml>=4.9
html5lib>=1.1
beautifulsoup4>=4.12

# WNBA data layer is pure ESPN JSON over stdlib urllib + requests — no
# vendor SDK / API key required.
